# Production-specific configuration for Spectrum KNX
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    # Use standard production restart policy
    restart: always
    # Mount the project volume but not development volumes
    volumes:
      - knx_project_data:/project

  db:
    restart: always
