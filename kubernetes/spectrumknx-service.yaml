apiVersion: v1
kind: Service
metadata:
  name: spectrumknx
  labels:
    app: spectrumknx
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: spectrumknx
